import { makeStyles } from "@material-ui/core";

export const useTabStyles = makeStyles(() => ({
  root: {
    margin: "20px",
    flexGrow: 1
  },
  appBar: {
      backgroundColor: "#ffff"
  }
}));
