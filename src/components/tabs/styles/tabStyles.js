import { makeStyles } from "@material-ui/core";

export const useTabStyles = makeStyles(() => ({
  root: {
    width:"100%",
    position:"static",
  
    flexGrow: 1
  },
  appBar: {
      backgroundColor: "#ffff"
  }
}));
